# Java Logical Programs

This folder contains 10 small Java programs demonstrating common logical exercises.

Compile all programs:

```bash
javac *.java
```

Run a program, for example:

```bash
java PalindromeCheck
```

Each class has a `main` with a sample input and printed output.
